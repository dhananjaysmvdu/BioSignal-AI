
name: Audit Provenance History

on:
  schedule:
    - cron: "0 5 * * 0"  # Sundays at 05:00 UTC

jobs:
  audit-history:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
    outputs:
      conf: ${{ steps.forecast.outputs.conf }}
      drift: ${{ steps.forecast.outputs.drift }}
      audit_depth: ${{ steps.policy.outputs.audit_depth }}
    steps:
      - name: Checkout (full history)
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install jq and gnupg
        run: |
          sudo apt-get update -y
          sudo apt-get install -y jq gnupg
          jq --version
          gpg --version

      - name: Import badge public key
        id: pubkey
        run: |
          if [ ! -f .github/public_keys/badge_signing.pub ]; then
            echo "Public key not found at .github/public_keys/badge_signing.pub" >&2
            exit 1
          fi
          gpg --import .github/public_keys/badge_signing.pub
          PUB_FPR=$(gpg --show-keys --with-colons .github/public_keys/badge_signing.pub | awk -F: '/^fpr:/{print $10; exit}')
          PUB_FPR_CLEAN=$(echo "$PUB_FPR" | tr -d ' ')
          echo "Imported public key fingerprint: $PUB_FPR"  # full forensics log
          echo "pub_fpr=$PUB_FPR_CLEAN" >> $GITHUB_OUTPUT

      - name: Audit last 10 provenance entries
        id: audit
        env:
          PUB_FPR: ${{ steps.pubkey.outputs.pub_fpr }}
        run: |
          set -e
          if [ ! -f reports/history/versions.json ]; then
            echo "versions.json not found at reports/history/versions.json" >&2
            echo "summary<<EOF" >> $GITHUB_OUTPUT
            echo "No versions.json present; nothing to audit." >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
            exit 0
          fi

          # Prepare entries (commit and expected sig hash) from various possible schemas
          mapfile -t ENTRIES < <(jq -r '
            ((.versions // .history // .entries // .) | (if type=="array" then . else [] end))
            | (if length>10 then .[-10:] else . end)
            | map({c: (.commit // .sha // .hash // .rev // .revision // .id), h: (.coverage_badge_hash // .coverageHash // .coverage_sig_sha256)})
            | map(select(.c!=null and .h!=null))
            | .[] | "\(.c) \(.h)"' reports/history/versions.json)

          PASS_CNT=0
          FAIL_CNT=0
          FAIL_LOG=""
          FAIL_COMMITS=""
          CHECKED=${#ENTRIES[@]}

          for line in "${ENTRIES[@]}"; do
            COMMIT=$(echo "$line" | awk '{print $1}')
            EXPECTED=$(echo "$line" | awk '{print $2}')
            echo "\nAuditing commit $COMMIT ..."

            # 1) Verify signature matches imported key
            SIG_OUT=$(git log -1 --show-signature "$COMMIT" 2>&1 || true)
            echo "$SIG_OUT"
            COMMIT_FPR=$(echo "$SIG_OUT" | sed -n -E 's/^gpg: Primary key fingerprint: (.*)/\1/p' | head -n1 | tr -d ' ')
            if [ -z "$COMMIT_FPR" ]; then
              echo "❌ No GPG fingerprint found in signature output for $COMMIT"
              FAIL_CNT=$((FAIL_CNT+1))
              FAIL_LOG+="- $COMMIT: missing signature or fingerprint\n"
              FAIL_COMMITS+="$COMMIT\n"
              continue
            fi
            if [ "$COMMIT_FPR" != "$PUB_FPR" ]; then
              echo "❌ Fingerprint mismatch: commit uses $COMMIT_FPR, expected $PUB_FPR"
              FAIL_CNT=$((FAIL_CNT+1))
              FAIL_LOG+="- $COMMIT: fingerprint mismatch\n"
              FAIL_COMMITS+="$COMMIT\n"
              continue
            fi
            echo "✅ Signature fingerprint matches imported key"

            echo "$SIG_OUT" | grep -q "Good signature from" || {
              echo "❌ Signature not valid for $COMMIT"; FAIL_CNT=$((FAIL_CNT+1)); FAIL_LOG+="- $COMMIT: signature invalid\n"; FAIL_COMMITS+="$COMMIT\n"; continue; }

            # 2) Verify coverage.sig hash at that revision
            if ! git cat-file -e "$COMMIT:badges/coverage.sig" 2>/dev/null; then
              echo "❌ badges/coverage.sig missing at $COMMIT"
              FAIL_CNT=$((FAIL_CNT+1))
              FAIL_LOG+="- $COMMIT: coverage.sig missing\n"
              FAIL_COMMITS+="$COMMIT\n"
              continue
            fi
            TMP=$(mktemp)
            git show "$COMMIT:badges/coverage.sig" > "$TMP"
            ACTUAL=$(sha256sum "$TMP" | awk '{print $1}')
            rm -f "$TMP"
            if [ "$ACTUAL" != "$EXPECTED" ]; then
              echo "❌ coverage.sig SHA256 mismatch (expected $EXPECTED, got $ACTUAL)"
              FAIL_CNT=$((FAIL_CNT+1))
              FAIL_LOG+="- $COMMIT: coverage.sig hash mismatch\n"
              FAIL_COMMITS+="$COMMIT\n"
              continue
            fi
            echo "✅ coverage.sig hash matches versions.json entry"

            PASS_CNT=$((PASS_CNT+1))
          done

          echo "pass=$PASS_CNT" >> $GITHUB_OUTPUT
          echo "fail=$FAIL_CNT" >> $GITHUB_OUTPUT
          echo "checked=$CHECKED" >> $GITHUB_OUTPUT
          # Preserve detailed failure list (may be empty)
          echo "fail_log<<EOF" >> $GITHUB_OUTPUT
          echo -e "$FAIL_LOG" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
          echo "fail_commits<<EOF" >> $GITHUB_OUTPUT
          echo -e "$FAIL_COMMITS" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

          # Step summary
          echo "### 🔎 Provenance Audit Summary" >> $GITHUB_STEP_SUMMARY
          echo "- Checked: $CHECKED" >> $GITHUB_STEP_SUMMARY
          echo "- Verified: $PASS_CNT" >> $GITHUB_STEP_SUMMARY
          echo "- Failed: $FAIL_CNT" >> $GITHUB_STEP_SUMMARY
          if [ "$FAIL_CNT" -gt 0 ]; then
            echo "\n#### Failures" >> $GITHUB_STEP_SUMMARY
            echo -e "$FAIL_LOG" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Open or update drift issue on failures
        if: ${{ steps.audit.outputs.fail && steps.audit.outputs.fail != '0' }}
        id: issue
        env:
          FAIL_LOG: ${{ steps.audit.outputs.fail_log }}
        run: |
          TITLE="Provenance drift detected"
          # Find existing open issue with same title
          EXISTING=$(curl -s -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/${{ github.repository }}/issues?state=open\&per_page=100 | \
            jq -r --arg t "$TITLE" '.[] | select(.title==$t) | .number' | head -n1)
          if [ -n "$EXISTING" ]; then
            echo "Existing issue #$EXISTING found. Appending comment."
            BODY="New failures detected by scheduled audit:\n\n${FAIL_LOG}"
            curl -s -X POST \
              -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
              -H "Accept: application/vnd.github+json" \
              https://api.github.com/repos/${{ github.repository }}/issues/$EXISTING/comments \
              -d "$(jq -nc --arg b "$BODY" '{body:$b}')"
            echo "issue_url=https://github.com/${{ github.repository }}/issues/$EXISTING" >> $GITHUB_OUTPUT
          else
            echo "Creating new issue: $TITLE"
            BODY="The scheduled provenance audit detected the following failures:\n\n${FAIL_LOG}"
            RESP=$(curl -s -X POST \
              -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
              -H "Accept: application/vnd.github+json" \
              https://api.github.com/repos/${{ github.repository }}/issues \
              -d "$(jq -nc --arg t "$TITLE" --arg b "$BODY" '{title:$t, body:$b}')")
            URL=$(echo "$RESP" | jq -r '.html_url')
            echo "issue_url=$URL" >> $GITHUB_OUTPUT
          fi

      - name: Auto-remediation (regenerate badge)
        if: ${{ steps.audit.outputs.fail && steps.audit.outputs.fail != '0' }}
        env:
          FAIL_COMMITS: ${{ steps.audit.outputs.fail_commits }}
        run: |
          echo "Triggering coverage_badge.yml via workflow_dispatch for remediation"
          curl -s -X POST \
            -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
            -H "Accept: application/vnd.github+json" \
            https://api.github.com/repos/${{ github.repository }}/actions/workflows/coverage_badge.yml/dispatches \
            -d '{"ref":"main"}'
          # Log per-commit remediation note
          echo "\n#### Auto-remediation" >> $GITHUB_STEP_SUMMARY
          while IFS= read -r c; do
            [ -n "$c" ] || continue
            echo "🛠️ Auto-remediation triggered for $c" | tee -a $GITHUB_STEP_SUMMARY
          done <<< "$FAIL_COMMITS"

      - name: Generate executive audit summary
        run: |
          mkdir -p reports
          TS=$(date -u +"%Y-%m-%d %H:%M:%S UTC")
          CHECKED=${{ steps.audit.outputs.checked || 0 }}
          PASSED=${{ steps.audit.outputs.pass || 0 }}
          FAILED=${{ steps.audit.outputs.fail || 0 }}
          REMEDIATED=${{ steps.audit.outputs.fail || 0 }}
          ISSUE_URL=${{ steps.issue.outputs.issue_url || '' }}
          WF_LINK="https://github.com/${{ github.repository }}/actions/workflows/coverage_badge.yml"
          STATUS=$([ "$FAILED" -gt 0 ] && echo "ATTENTION REQUIRED" || echo "PASS")
          {
            echo "# Weekly Provenance Audit Summary"
            echo
            echo "- Timestamp: $TS"
            echo "- Auditor: GitHub Actions bot"
            echo "- Commits checked: $CHECKED"
            echo "- Passed: $PASSED"
            echo "- Failed: $FAILED"
            echo "- Remediated: $REMEDIATED"
            echo "- Issue: ${ISSUE_URL:-N/A}"
            echo "- Workflow: $WF_LINK"
            echo "- Overall Status: $STATUS"
          } > reports/audit_summary.md
          # Append dashboard link into step summary now (generated next)
          echo "\n[Provenance Dashboard](reports/provenance_dashboard.html)" >> $GITHUB_STEP_SUMMARY

      - name: Generate provenance insights dashboard
        run: |
          python scripts/workflow_utils/generate_provenance_dashboard.py
      - name: Forecast provenance risk
        id: forecast
        run: |
          python scripts/workflow_utils/forecast_provenance_risk.py > forecast_output.json
          DRIFT=$(jq -r '.predicted_drift_probability_pct' forecast_output.json || echo 0)
          REM=$(jq -r '.expected_remediations' forecast_output.json || echo 0)
          CONF=$(jq -r '.confidence_pct' forecast_output.json || echo 0)
          echo "drift=$DRIFT" >> $GITHUB_OUTPUT
          echo "rem=$REM" >> $GITHUB_OUTPUT
          echo "conf=$CONF" >> $GITHUB_OUTPUT
          echo "\n🔮 Next-week drift probability: ${DRIFT}%" >> $GITHUB_STEP_SUMMARY
          echo "🧩 Confidence: ${CONF}%" >> $GITHUB_STEP_SUMMARY
          echo "⚙️ Expected remediations: ${REM}" >> $GITHUB_STEP_SUMMARY
      - name: Adaptive governance tuning
        id: policy
        run: |
          python scripts/workflow_utils/prescriptive_controller.py > policy_output.json
          AUDIT_DEPTH=$(jq -r '.policy.audit_depth' policy_output.json || echo unknown)
          DRIFT_THRESH=$(jq -r '.policy.drift_threshold' policy_output.json || echo 0)
          NEXT_AUDIT=$(jq -r '.policy.next_audit_date' policy_output.json || echo unknown)
          echo "audit_depth=$AUDIT_DEPTH" >> $GITHUB_OUTPUT
          echo "drift_threshold=$DRIFT_THRESH" >> $GITHUB_OUTPUT
          echo "next_audit=$NEXT_AUDIT" >> $GITHUB_OUTPUT
          echo "⚖️ Audit depth set to: $AUDIT_DEPTH" >> $GITHUB_STEP_SUMMARY
          echo "🧮 New drift threshold: $DRIFT_THRESH" >> $GITHUB_STEP_SUMMARY
          echo "🕒 Next scheduled audit: $NEXT_AUDIT" >> $GITHUB_STEP_SUMMARY

      - name: Explain policy decision
        run: |
          python scripts/workflow_utils/explain_policy_adjustment.py > rationale_output.txt
          echo "📜 Generated compliance explanation for latest policy decision" >> $GITHUB_STEP_SUMMARY

      - name: Commit executive audit summary
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/audit_summary.md reports/provenance_dashboard.html reports/provenance_forecast.json configs/governance_policy.json reports/compliance_rationale.md logs/decision_trace.json
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "ci: add explainable compliance rationale for adaptive governance."
            git push
          fi

  human-oversight:
    needs: [audit-history]
    if: ${{ needs.audit-history.outputs.conf != '' && (fromJSON(needs.audit-history.outputs.conf) < 80 || fromJSON(needs.audit-history.outputs.drift) >= 30 || needs.audit-history.outputs.audit_depth == 'moderate' || needs.audit-history.outputs.audit_depth == 'full') }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run human oversight gateway
        id: gateway
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: |
          python scripts/workflow_utils/human_oversight_gateway.py
          echo "🧠 Human review initiated for high-risk policy change." >> $GITHUB_STEP_SUMMARY
      - name: Generate standardized review template
        id: template
        env:
          REVIEWER: ${{ github.triggering_actor }}
        run: |
          python scripts/workflow_utils/generate_review_template.py > /dev/null
          DID=$(jq -r '.decision_id' "$GITHUB_STEP_SUMMARY" 2>/dev/null || echo "")
          echo "📝 Generated standardized review form." >> $GITHUB_STEP_SUMMARY

      - name: Update audit summary with pending review
        env:
          CONF: ${{ needs.audit-history.outputs.conf }}
          DEPTH: ${{ needs.audit-history.outputs.audit_depth }}
        run: |
          mkdir -p reports
          DID=$(date -u +"%Y%m%dT%H%M%SZ")
          RISK=$([ "$DEPTH" = "full" ] && echo "high" || ([ "$DEPTH" = "moderate" ] && echo "medium" || echo "low"))
          ASSIGNEE="unassigned"
          {
            echo ""
            echo "## Pending Review:"
            echo "Decision ID | Risk | Confidence | Assigned Reviewer"
            echo "---|---|---:|---"
            echo "$DID | $RISK | ${CONF}% | $ASSIGNEE"
          } >> reports/audit_summary.md

      - name: Commit review request and audit summary
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/review_request.md reports/reviews/pending/* reports/audit_summary.md logs/decision_trace.json
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "ci: add standardized human review template generator for oversight consistency"
            git push
          fi

  record-human-review:
    needs: [human-oversight]
    if: ${{ always() }}
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Record human review outcomes
        id: record
        run: |
          python scripts/workflow_utils/record_human_review.py
      - name: Commit governance ledger updates
        run: |
          if [ -f logs/oversight_ledger.json ]; then
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git add logs/oversight_ledger.json reports/audit_summary.md reports/reviews/completed/* || true
            if git diff --cached --quiet; then
              echo "No ledger updates to commit."
            else
              git commit -m "ci: record human oversight outcomes and update governance ledger"
              git push
            fi
          fi

  learn-meta:
    needs: [record-human-review]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Integrate human feedback into policy
        run: |
          python scripts/workflow_utils/learn_from_reviews.py > meta_out.json
          echo "🧠 Learned from human oversight feedback." >> $GITHUB_STEP_SUMMARY
      - name: Commit meta-learning updates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add configs/governance_policy.json reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No meta-learning changes to commit."
          else
            git commit -m "ci: integrate human feedback into adaptive governance learning loop"
            git push
          fi

  score-reviewers:
    needs: [learn-meta]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      trust_index: ${{ steps.trust.outputs.trust_index }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock

      - name: Run reviewer reliability scoring
        run: |
          python scripts/workflow_utils/score_reviewer_reliability.py

      - name: Regenerate provenance dashboard
        run: |
          python scripts/workflow_utils/generate_provenance_dashboard.py

      - name: Compute and publish trust index (optional)
        id: trust
        shell: bash
        run: |
          python - <<'PY'
          import json, os
          def load_json(p, default=None):
              try:
                  with open(p,'r',encoding='utf-8') as f:
                      return json.load(f)
              except Exception:
                  return default
          scores = load_json('logs/reviewer_scores.json', {}) or {}
          ledger = load_json('logs/oversight_ledger.json', []) or []
          alignments = [float(v.get('alignment',0.0)) for v in (scores.values() if isinstance(scores, dict) else [])]
          mean_alignment = (sum(alignments)/len(alignments)) if alignments else 0.0
          approvals = sum(1 for e in ledger if str(e.get('verdict','')).lower()=='approved')
          total = len(ledger)
          disagreement_rate = 1.0 - (approvals/total) if total else 0.0
          trust_index = max(0.0, min(1.0, mean_alignment * (1.0 - disagreement_rate)))
          # Export as percentage with 2 decimals
          with open(os.environ['GITHUB_OUTPUT'],'a', encoding='utf-8') as g:
              g.write(f"trust_index={trust_index*100:.2f}\n")
          with open(os.environ['GITHUB_STEP_SUMMARY'],'a', encoding='utf-8') as s:
              s.write(f"\n🔐 Oversight Trust Index: {trust_index*100:.2f}%\n")
          PY

      - name: Commit reviewer metrics and dashboard
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add logs/reviewer_scores.json reports/provenance_dashboard.html reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No reviewer metrics or dashboard changes to commit."
          else
            git commit -m "ci: integrate reviewer reliability scoring and regenerate provenance dashboard"
            git push
          fi

      - name: Append run summary
        run: |
          echo "📊 Reviewer reliability metrics updated — Oversight Trust Index recalculated." >> $GITHUB_STEP_SUMMARY

  generate-trust-badge:
    needs: [score-reviewers]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Generate Oversight Trust Index badge
        id: badge
        run: |
          python scripts/workflow_utils/generate_trust_badge.py > trust_badge.json
          TRUST=$(jq -r '.trust_index_pct' trust_badge.json || echo 0)
          echo "Trust Index: ${TRUST}%" >> $GITHUB_STEP_SUMMARY
      - name: Verify trust badge signature
        run: |
          cd badges
          sha256sum -c trust_index.sig
      - name: Commit trust badge and summary
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add badges/trust_index.svg badges/trust_index.sig README.md reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No trust badge changes to commit."
          else
            git commit -m "ci: publish Oversight Trust Index badge"
            git push
          fi

  update-policy-from-trust:
    needs: [generate-trust-badge]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Adapt governance policy from Trust Index
        id: adapt
        run: |
          python scripts/workflow_utils/update_policy_from_trust_index.py > trust_policy.json
          TI=$(jq -r '.trust_index_pct' trust_policy.json || echo 0)
          echo "trust_index=${TI}" >> $GITHUB_OUTPUT
          echo "⚙️ Governance policy adapted based on Oversight Trust Index (${TI}%)." >> $GITHUB_STEP_SUMMARY
      - name: Commit adapted policy
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add configs/governance_policy.json reports/audit_summary.md logs/decision_trace.json
          if git diff --cached --quiet; then
            echo "No governance policy changes to commit."
          else
            git commit -m "ci: adapt governance policy based on Oversight Trust Index"
            git push
          fi

  compute-governance-health:
    needs: [update-policy-from-trust]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Compute Governance Health Score
        id: ghs
        run: |
          python scripts/workflow_utils/compute_governance_health.py > ghs_output.json
          GHS=$(jq -r '.GovernanceHealthScore' ghs_output.json || echo 0)
          echo "ghs=${GHS}" >> $GITHUB_OUTPUT
          STATUS="Critical"
          if [ "$(printf '%.0f' "$GHS")" -ge 80 ]; then STATUS="Healthy"; elif [ "$(printf '%.0f' "$GHS")" -ge 60 ]; then STATUS="Attention"; fi
          echo "🧮 Governance Health Score: ${GHS}%" >> $GITHUB_STEP_SUMMARY
          echo "✅ Status: ${STATUS}" >> $GITHUB_STEP_SUMMARY
      - name: Verify governance health badge signature
        run: |
          cd badges
          sha256sum -c governance_health.sig
      - name: Commit governance health artifacts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/governance_health.json badges/governance_health.svg badges/governance_health.sig reports/audit_summary.md README.md
          if git diff --cached --quiet; then
            echo "No governance health changes to commit."
          else
            git commit -m "ci: compute and publish composite Governance Health Score and badge"
            git push
          fi

  update-governance-cycle:
    needs: [compute-governance-health]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Adapt governance cycle from GHS
        id: cycle
        run: |
          python scripts/workflow_utils/update_governance_cycle.py > cycle_output.json
          GHS=$(jq -r '.ghs' cycle_output.json || echo 0)
          FREQ=$(jq -r '.audit_frequency' cycle_output.json || echo unknown)
          echo "ghs=${GHS}" >> $GITHUB_OUTPUT
          echo "audit_frequency=${FREQ}" >> $GITHUB_OUTPUT
          echo "🔁 Governance cycle updated based on GHS = ${GHS}%" >> $GITHUB_STEP_SUMMARY
          echo "🕒 Next audit in ${FREQ}" >> $GITHUB_STEP_SUMMARY
      - name: Commit governance cycle changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add configs/governance_policy.json logs/decision_trace.json reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No governance cycle changes to commit."
          else
            git commit -m "ci: adapt governance meta-learning and audit schedule from Governance Health Score"
            git push
          fi
      - name: Regenerate Governance Pulse dashboard
        run: |
          python scripts/workflow_utils/generate_provenance_dashboard.py
      - name: Commit updated dashboard and summary
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/provenance_dashboard.html reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No dashboard changes to commit."
          else
            git commit -m "ci: update Governance Pulse dashboard with temporal adaptation visualization"
            git push
          fi
      - name: Append dashboard update note
        run: |
          echo "📈 Governance Pulse dashboard updated — GHS trend and cadence timeline refreshed." >> $GITHUB_STEP_SUMMARY

  record-governance-history:
    needs: [update-governance-cycle]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Record governance history snapshot
        run: |
          python scripts/workflow_utils/record_governance_history.py > gov_hist.json
          COUNT=$(jq -r '.written' gov_hist.json || echo 0)
          echo "🧭 Recorded governance history snapshot — timeline extended (entries=${COUNT})." >> $GITHUB_STEP_SUMMARY
      - name: Commit governance history
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/history/versions.json
          if git diff --cached --quiet; then
            echo "No governance history changes to commit."
          else
            git commit -m "ci: record governance history snapshot for Governance Pulse timeline"
            git push
          fi
      - name: Regenerate dashboard with coefficient trends
        run: |
          python scripts/workflow_utils/generate_provenance_dashboard.py
      - name: Commit updated dashboard (meta-learning trends)
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/provenance_dashboard.html
          if git diff --cached --quiet; then
            echo "No dashboard coefficient trend changes to commit."
          else
            git commit -m "ci: extend governance history and pulse dashboard with meta-learning coefficient trends"
            git push
          fi
      - name: Append coefficient trends summary
        run: |
          echo "🧪 Meta-learning coefficient trends plotted on Governance Pulse dashboard." >> $GITHUB_STEP_SUMMARY


  compute-meta-stability:
    needs: [record-governance-history]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Compute meta-stability index
        id: msi
        run: |
          python scripts/workflow_utils/compute_meta_stability.py > msi_out.json
          MSI=$(jq -r '.meta_stability_index' msi_out.json || echo 0)
          echo "msi=${MSI}" >> $GITHUB_OUTPUT
          echo "🧠 Meta-Stability Index computed: ${MSI}%" >> $GITHUB_STEP_SUMMARY
      - name: Regenerate dashboard with stability gauge
        run: |
          python scripts/workflow_utils/generate_provenance_dashboard.py
      - name: Commit meta-stability artifacts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/meta_stability.json reports/audit_summary.md reports/provenance_dashboard.html
          if git diff --cached --quiet; then
            echo "No meta-stability changes to commit."
          else
            git commit -m "ci: add meta-stability index and learning stability gauge to governance dashboard"
            git push
          fi
      - name: Append stability gauge summary
        run: |
          echo "📊 Meta-Stability Index computed — learning stability gauge updated." >> $GITHUB_STEP_SUMMARY

  trust-correlation-analyzer:
    needs: [compute-meta-stability]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Run Trust–Health Correlation Analyzer
        run: |
          python scripts/workflow_utils/analyze_trust_correlation.py
      - name: Regenerate dashboard with trust correlation panel
        run: |
          python scripts/workflow_utils/generate_provenance_dashboard.py
      - name: Commit trust correlation artifacts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/trust_correlation.json reports/audit_summary.md reports/provenance_dashboard.html
          if git diff --cached --quiet; then
            echo "No trust correlation changes to commit."
          else
            git commit -m "ci: add Trust–Health Correlation Analyzer and dashboard visualization"
            git push
          fi
      - name: Append trust correlation summary
        run: |
          echo "🤝 Trust–Health Correlation analysis completed — GHS/MSI alignment quantified." >> $GITHUB_STEP_SUMMARY

  trust-correlation-controller:
    needs: [trust-correlation-analyzer]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Adjust trust weighting from correlation
        id: adjust
        run: |
          python scripts/workflow_utils/trust_correlation_controller.py > trust_ctrl_out.json
          NEW_WEIGHT=$(jq -r '.new_weight' trust_ctrl_out.json || echo 0.5)
          ADJ=$(jq -r '.adjustment' trust_ctrl_out.json || echo 0.0)
          echo "new_weight=${NEW_WEIGHT}" >> $GITHUB_OUTPUT
          echo "adjustment=${ADJ}" >> $GITHUB_OUTPUT
      - name: Commit trust weighting updates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add configs/governance_policy.json reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No trust weighting changes to commit."
          else
            git commit -m "ci: adjust trust weighting based on correlation analysis"
            git push
          fi
      - name: Append trust weighting summary
        run: |
          echo "⚖️ Trust weighting adjusted from correlation insight (new weight: ${{ steps.adjust.outputs.new_weight }}, Δ${{ steps.adjust.outputs.adjustment }})." >> $GITHUB_STEP_SUMMARY

  governance-coherence-analyzer:
    needs: [trust-correlation-controller]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Analyze governance coherence
        id: coherence
        run: |
          python scripts/workflow_utils/governance_coherence_analyzer.py > coherence_out.json
          INDEX=$(jq -r '.coherence_index' coherence_out.json || echo 100)
          STATUS=$(jq -r '.status' coherence_out.json || echo 'Stable')
          echo "coherence_index=${INDEX}" >> $GITHUB_OUTPUT
          echo "status=${STATUS}" >> $GITHUB_OUTPUT
      - name: Commit coherence analysis
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/governance_coherence.json reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No coherence analysis changes to commit."
          else
            git commit -m "ci: analyze governance parameter coherence"
            git push
          fi
      - name: Append coherence summary
        run: |
          echo "🧩 Governance coherence evaluated (index ${{ steps.coherence.outputs.coherence_index }}% — ${{ steps.coherence.outputs.status }})." >> $GITHUB_STEP_SUMMARY

  governance-equilibrium-predictor:
    needs: [governance-coherence-analyzer]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Forecast governance equilibrium
        id: equilibrium
        run: |
          python scripts/workflow_utils/governance_equilibrium_predictor.py > equilibrium_out.json
          TREND=$(jq -r '.trend' equilibrium_out.json || echo 'neutral')
          CYCLES=$(jq -r '.predicted_equilibrium_cycles' equilibrium_out.json || echo 999)
          CONFIDENCE=$(jq -r '.confidence' equilibrium_out.json || echo 0.5)
          echo "trend=${TREND}" >> $GITHUB_OUTPUT
          echo "cycles=${CYCLES}" >> $GITHUB_OUTPUT
          echo "confidence=${CONFIDENCE}" >> $GITHUB_OUTPUT
      - name: Commit equilibrium forecast
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/governance_equilibrium.json reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No equilibrium forecast changes to commit."
          else
            git commit -m "ci: forecast governance equilibrium state"
            git push
          fi
      - name: Append equilibrium summary
        run: |
          echo "🔮 Governance equilibrium forecast generated — trend: ${{ steps.equilibrium.outputs.trend }} (~${{ steps.equilibrium.outputs.cycles }} cycles, confidence ${{ steps.equilibrium.outputs.confidence }})." >> $GITHUB_STEP_SUMMARY

  governance-stabilization-planner:
    needs: [governance-equilibrium-predictor]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Execute stabilization planning
        id: stabilize
        run: |
          python scripts/workflow_utils/governance_stabilization_planner.py > stabilize_out.json
          MODE=$(jq -r '.stabilization_mode' stabilize_out.json || echo 'monitor')
          ACTION=$(jq -r '.action_taken' stabilize_out.json || echo 'No action')
          CHANGED=$(jq -r '.changes_applied' stabilize_out.json || echo 'false')
          echo "mode=${MODE}" >> $GITHUB_OUTPUT
          echo "action=${ACTION}" >> $GITHUB_OUTPUT
          echo "changed=${CHANGED}" >> $GITHUB_OUTPUT
      - name: Commit stabilization plan
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add configs/governance_policy.json logs/decision_trace.json reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No stabilization plan changes to commit."
          else
            git commit -m "ci: apply proactive governance stabilization plan"
            git push
          fi
      - name: Append stabilization summary
        run: |
          echo "🛠️ Governance stabilization plan executed — mode: ${{ steps.stabilize.outputs.mode }}, action: ${{ steps.stabilize.outputs.action }}." >> $GITHUB_STEP_SUMMARY

  governance-memory-consolidator:
    needs: [governance-stabilization-planner]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Consolidate governance memory
        id: memory
        run: |
          python scripts/workflow_utils/governance_memory_consolidator.py > memory_out.json
          CYCLES=$(jq -r '.cycles_analyzed' memory_out.json || echo 0)
          TREND=$(jq -r '.trends.overall' memory_out.json || echo 'unknown')
          CONFLICTS=$(jq -r '.recurrent_conflicts | length' memory_out.json || echo 0)
          echo "cycles=${CYCLES}" >> $GITHUB_OUTPUT
          echo "trend=${TREND}" >> $GITHUB_OUTPUT
          echo "conflicts=${CONFLICTS}" >> $GITHUB_OUTPUT
      - name: Commit governance memory artifacts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/governance_memory.json reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No governance memory changes to commit."
          else
            git commit -m "ci: consolidate governance memory and extract stability patterns"
            git push
          fi
      - name: Append memory consolidation summary
        run: |
          echo "🧠 Governance memory consolidated — analyzed ${{ steps.memory.outputs.cycles }} cycles, trend: ${{ steps.memory.outputs.trend }}, conflicts: ${{ steps.memory.outputs.conflicts }}." >> $GITHUB_STEP_SUMMARY

  governance-meta-reflection:
    needs: [governance-memory-consolidator]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Generate meta-reflection report
        id: reflect
        run: |
          python scripts/workflow_utils/governance_meta_reflection.py > reflection_out.json
          MODE=$(jq -r '.dominant_mode' reflection_out.json || echo 'monitor')
          TREND=$(jq -r '.overall_trend' reflection_out.json || echo 'stable')
          COHERENCE=$(jq -r '.coherence_index' reflection_out.json || echo 100)
          echo "mode=${MODE}" >> $GITHUB_OUTPUT
          echo "trend=${TREND}" >> $GITHUB_OUTPUT
          echo "coherence=${COHERENCE}" >> $GITHUB_OUTPUT
      - name: Commit meta-reflection artifacts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/governance_meta_reflection.md reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No meta-reflection changes to commit."
          else
            git commit -m "ci: generate meta-reflection report on governance evolution"
            git push
          fi
      - name: Append meta-reflection summary
        run: |
          echo "🪞 Governance meta-reflection generated — mode: ${{ steps.reflect.outputs.mode }}, trend: ${{ steps.reflect.outputs.trend }}, coherence: ${{ steps.reflect.outputs.coherence }}%." >> $GITHUB_STEP_SUMMARY

  governance-archetype-classifier:
    needs: [governance-meta-reflection]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Classify governance archetype
        id: arche
        run: |
          python scripts/workflow_utils/governance_archetype_classifier.py > archetype_out.json
          NAME=$(jq -r '.archetype' archetype_out.json || echo 'Unknown Archetype')
          CONF=$(jq -r '.confidence' archetype_out.json || echo 0)
          echo "name=${NAME}" >> $GITHUB_OUTPUT
          echo "confidence=${CONF}" >> $GITHUB_OUTPUT
      - name: Commit archetype classification
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/governance_archetype.json reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No archetype classification changes to commit."
          else
            git commit -m "ci: classify current governance archetype and record semantic state"
            git push
          fi
      - name: Append archetype summary
        run: |
          echo "🏷️ Governance archetype classified — ${{ steps.arche.outputs.name }} (confidence ${{ steps.arche.outputs.confidence }}%)." >> $GITHUB_STEP_SUMMARY

  governance-archetype-transition-mapper:
    needs: [governance-archetype-classifier]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Map archetype transitions
        id: map
        run: |
          python scripts/workflow_utils/governance_archetype_transition_mapper.py > map_out.json
          FROM=$(jq -r '.latest_from' map_out.json || echo 'Unknown')
          TO=$(jq -r '.latest_to' map_out.json || echo 'Unknown')
          CONF=$(jq -r '.confidence' map_out.json || echo 0)
          echo "from=${FROM}" >> $GITHUB_OUTPUT
          echo "to=${TO}" >> $GITHUB_OUTPUT
          echo "confidence=${CONF}" >> $GITHUB_OUTPUT
      - name: Commit transition artifacts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add logs/archetype_history.json reports/archetype_transitions.json reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No archetype transition changes to commit."
          else
            git commit -m "ci: map governance archetype transitions and detect phase shifts"
            git push
          fi
      - name: Append transition summary
        run: |
          echo "🔄 Archetype transition mapped — latest: ${{ steps.map.outputs.from }} → ${{ steps.map.outputs.to }}, confidence ${{ steps.map.outputs.confidence }}%." >> $GITHUB_STEP_SUMMARY

  generate-archetype-dynamics-dashboard:
    needs: [governance-archetype-transition-mapper]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Generate archetype dynamics dashboard
        id: dynamics
        run: |
          python scripts/workflow_utils/generate_archetype_dynamics_dashboard.py > dynamics_out.json
          TRANS=$(jq -r '.transitions' dynamics_out.json || echo 0)
          PATH=$(jq -r '.dominant_path' dynamics_out.json || echo 'N/A')
          PVAL=$(jq -r '.p' dynamics_out.json || echo 0)
          echo "transitions=${TRANS}" >> $GITHUB_OUTPUT
          echo "dominant_path=${PATH}" >> $GITHUB_OUTPUT
          echo "p=${PVAL}" >> $GITHUB_OUTPUT
      - name: Commit dynamics dashboard artifacts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/archetype_dynamics_dashboard.html reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No archetype dynamics changes to commit."
          else
            git commit -m "ci: generate archetype dynamics visualization and summary"
            git push
          fi
      - name: Append archetype dynamics summary
        run: |
          echo "🧭 Archetype dynamics dashboard updated — transition matrix and dwell timeline refreshed (transitions: ${{ steps.dynamics.outputs.transitions }}, dominant: ${{ steps.dynamics.outputs.dominant_path }} p=${{ steps.dynamics.outputs.p }})." >> $GITHUB_STEP_SUMMARY

  governance-regime-stability-analyzer:
    needs: [generate-archetype-dynamics-dashboard]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Analyze regime stability
        id: regime
        run: |
          python scripts/workflow_utils/governance_regime_stability_analyzer.py > regime_out.json
          STAB=$(jq -r '.stability_index' regime_out.json || echo 0)
          STATUS=$(jq -r '.status' regime_out.json || echo 'unknown')
          echo "stability=${STAB}" >> $GITHUB_OUTPUT
          echo "status=${STATUS}" >> $GITHUB_OUTPUT
      - name: Commit regime stability artifacts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/regime_stability.json reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No regime stability changes to commit."
          else
            git commit -m "ci: analyze governance regime stability and volatility"
            git push
          fi
      - name: Append regime stability summary
        run: |
          echo "🌐 Regime stability evaluated — status ${{ steps.regime.outputs.status }} (index ${{ steps.regime.outputs.stability }}%)." >> $GITHUB_STEP_SUMMARY
  governance-regime-stability-monitor:
    needs: [governance-regime-stability-analyzer]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Monitor regime stability & generate gauge
        id: regime_monitor
        run: |
          python scripts/workflow_utils/governance_regime_stability_monitor.py > regime_monitor_out.json
          RSI=$(jq -r '.rsi' regime_monitor_out.json || echo 0)
          STATUS=$(jq -r '.status' regime_monitor_out.json || echo 'unknown')
          echo "rsi=${RSI}" >> $GITHUB_OUTPUT
          echo "regime_status=${STATUS}" >> $GITHUB_OUTPUT
      - name: Commit regime stability monitor artifacts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/regime_stability_gauge.html reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No regime stability monitor changes to commit."
          else
            git commit -m "ci: monitor regime stability and generate alert visualization"
            git push
          fi
      - name: Append regime stability monitor summary
        run: |
          echo "⚠️ Regime stability monitor executed — current RSI ${{ steps.regime_monitor.outputs.rsi }}%, status ${{ steps.regime_monitor.outputs.regime_status }}." >> $GITHUB_STEP_SUMMARY
  adaptive-learning-rate:
    needs: [governance-regime-stability-monitor]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Apply adaptive learning rate control
        id: adapt
        run: |
          python scripts/workflow_utils/adaptive_learning_rate_controller.py > alrc_out.json
          RATE=$(jq -r '.learning_rate_factor' alrc_out.json || echo 1.0)
          MSI=$(jq -r '.msi' alrc_out.json || echo 0)
          echo "rate_factor=${RATE}" >> $GITHUB_OUTPUT
          echo "msi=${MSI}" >> $GITHUB_OUTPUT
          echo "⚖️ Adaptive meta-learning rate adjusted: MSI=${MSI}% → ×${RATE}" >> $GITHUB_STEP_SUMMARY
      - name: Commit adaptive learning rate updates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add configs/governance_policy.json reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No adaptive learning rate changes to commit."
          else
            git commit -m "ci: apply adaptive meta-learning rate control based on meta-stability feedback"
            git push
          fi
      - name: Append adaptive rate summary
        run: |
          echo "⚖️ Adaptive meta-learning rate adjusted from MSI feedback." >> $GITHUB_STEP_SUMMARY

  evaluate-adaptation-outcomes:
    needs: [adaptive-learning-rate]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Evaluate adaptation outcomes
        id: evaluate
        run: |
          python scripts/workflow_utils/evaluate_adaptation_outcomes.py > eval_out.json
          OUTCOME=$(jq -r '.outcome' eval_out.json || echo 'neutral')
          GHS_DELTA=$(jq -r '.ghs_delta' eval_out.json || echo 0)
          MSI_DELTA=$(jq -r '.msi_delta' eval_out.json || echo 0)
          echo "outcome=${OUTCOME}" >> $GITHUB_OUTPUT
          echo "ghs_delta=${GHS_DELTA}" >> $GITHUB_OUTPUT
          echo "msi_delta=${MSI_DELTA}" >> $GITHUB_OUTPUT
          echo "📊 Evaluated last adaptation — outcome classified as ${OUTCOME^} (GHS Δ${GHS_DELTA}, MSI Δ${MSI_DELTA})." >> $GITHUB_STEP_SUMMARY
      - name: Commit adaptation outcome evaluation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add logs/adaptation_outcomes.json reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No adaptation outcome changes to commit."
          else
            git commit -m "ci: record and evaluate adaptation outcomes for governance self-assessment"
            git push
          fi
      - name: Append evaluation summary
        run: |
          OUTCOME="${{ steps.evaluate.outputs.outcome }}"
          echo "📊 Evaluated last adaptation — outcome classified as ${OUTCOME^}." >> $GITHUB_STEP_SUMMARY

  weighted-adaptation-optimizer:
    needs: [evaluate-adaptation-outcomes]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Apply weighted optimization
        id: optimize
        run: |
          python scripts/workflow_utils/weighted_adaptation_optimizer.py > wao_out.json
          STATUS=$(jq -r '.status' wao_out.json || echo 'unknown')
          SUCCESS_RATE=$(jq -r '.success_rate' wao_out.json || echo 0)
          IMPROVED=$(jq -r '.improved_count' wao_out.json || echo 0)
          echo "status=${STATUS}" >> $GITHUB_OUTPUT
          echo "success_rate=${SUCCESS_RATE}" >> $GITHUB_OUTPUT
          if [ "$STATUS" = "applied" ]; then
            echo "🧩 Weighted optimization applied — success rate ${SUCCESS_RATE}% (${IMPROVED} improved outcomes)." >> $GITHUB_STEP_SUMMARY
          else
            REASON=$(jq -r '.reason' wao_out.json || echo 'unknown')
            echo "⏭️ Weighted optimization skipped — ${REASON}." >> $GITHUB_STEP_SUMMARY
          fi
      - name: Commit weighted optimization updates
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add configs/governance_policy.json reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No weighted optimization changes to commit."
          else
            git commit -m "ci: apply outcome-weighted optimization to governance coefficients"
            git push
          fi
      - name: Append optimization summary
        run: |
          STATUS="${{ steps.optimize.outputs.status }}"
          if [ "$STATUS" = "applied" ]; then
            echo "🧩 Weighted adaptation optimization applied — coefficients updated from historical success memory." >> $GITHUB_STEP_SUMMARY
          fi

  causal-influence-analyzer:
    needs: [weighted-adaptation-optimizer]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Compute causal influence
        id: causal
        run: |
          python scripts/workflow_utils/causal_influence_analyzer.py > causal_out.json
          USABLE=$(jq -r '.usable_samples' causal_out.json || echo 0)
          CONF=$(jq -r '.confidence' causal_out.json || echo 0)
          NOTE=$(jq -r '.note' causal_out.json || echo '')
          echo "usable_samples=${USABLE}" >> $GITHUB_OUTPUT
          echo "confidence=${CONF}" >> $GITHUB_OUTPUT
          if [ "$USABLE" -ge 5 ]; then
            echo "🔍 Causal influence analysis completed — identified key stabilizing parameters." >> $GITHUB_STEP_SUMMARY
          else
            echo "🔍 Causal influence analysis neutral — insufficient data (${USABLE} samples)." >> $GITHUB_STEP_SUMMARY
          fi
      - name: Commit causal influence artifacts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/causal_influence.json reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No causal influence changes to commit."
          else
            git commit -m "ci: compute causal influence of coefficient changes on governance performance"
            git push
          fi
      - name: Append causal summary
        run: |
          echo "Confidence: ${{ steps.causal.outputs.confidence }}% (usable samples: ${{ steps.causal.outputs.usable_samples }})" >> $GITHUB_STEP_SUMMARY

  predictive-adaptation-planner:
    needs: [causal-influence-analyzer]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Predict adaptation outcomes
        id: predict
        run: |
          python scripts/workflow_utils/predictive_adaptation_planner.py > predict_out.json
          STATUS=$(jq -r '.status' predict_out.json || echo 'ok')
          IMP=$(jq -r '.predicted_improvement_percent' predict_out.json || echo 0)
          CONF=$(jq -r '.confidence' predict_out.json || echo 0)
          echo "status=${STATUS}" >> $GITHUB_OUTPUT
          echo "improvement=${IMP}" >> $GITHUB_OUTPUT
          echo "confidence=${CONF}" >> $GITHUB_OUTPUT
          echo "� Predictive adaptation plan generated — next-cycle coefficients estimated." >> $GITHUB_STEP_SUMMARY
      - name: Commit prediction forecast
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/adaptation_plan.json reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No prediction changes to commit."
          else
            git commit -m "ci: generate predictive adaptation plan using causal influence analysis"
            git push
          fi
      - name: Append prediction summary
        run: |
          IMP="${{ steps.predict.outputs.improvement }}"
          CONF="${{ steps.predict.outputs.confidence }}"
          echo "� Planned improvement: ${IMP}% (confidence ${CONF}%)." >> $GITHUB_STEP_SUMMARY

  calibrate-prediction-accuracy:
    needs: [predictive-adaptation-planner]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Generate dashboard & calibrate prediction
        id: calibrate
        run: |
          python scripts/workflow_utils/generate_provenance_dashboard.py || echo "Dashboard generation failed but continuing"
          python scripts/workflow_utils/calibrate_prediction_accuracy.py > calib_out.json
          MEAN_ERR=$(jq -r '.mean_abs_error_pct' calib_out.json || echo 0)
          REL=$(jq -r '.reliability' calib_out.json || echo 'Unknown')
          echo "mean_err=${MEAN_ERR}" >> $GITHUB_OUTPUT
          echo "reliability=${REL}" >> $GITHUB_OUTPUT
          echo "🧭 Predictive calibration updated — forecast accuracy refined." >> $GITHUB_STEP_SUMMARY
      - name: Commit calibration artifacts
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add reports/prediction_calibration.json reports/audit_summary.md reports/provenance_dashboard.html
          if git diff --cached --quiet; then
            echo "No calibration changes to commit."
          else
            git commit -m "ci: update predictive adaptation dashboard and calibration metrics"
            git push
          fi
      - name: Append calibration summary
        run: |
          echo "Forecast mean absolute error: ${{ steps.calibrate.outputs.mean_err }}% (Reliability: ${{ steps.calibrate.outputs.reliability }})." >> $GITHUB_STEP_SUMMARY

  calibrate-forecast-accuracy:
    needs: [calibrate-prediction-accuracy]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Calibrate forecast reliability
        id: forecast
        run: |
          python scripts/workflow_utils/calibrate_forecast_accuracy.py > forecast_out.json
          STATUS=$(jq -r '.status' forecast_out.json || echo 'unknown')
          ERR=$(jq -r '.error' forecast_out.json || echo 0)
          ACC=$(jq -r '.accuracy' forecast_out.json || echo 0)
          REL=$(jq -r '.reliability' forecast_out.json || echo 0)
          echo "status=${STATUS}" >> $GITHUB_OUTPUT
          echo "error=${ERR}" >> $GITHUB_OUTPUT
          echo "accuracy=${ACC}" >> $GITHUB_OUTPUT
          echo "reliability=${REL}" >> $GITHUB_OUTPUT
          echo "🎯 Forecast calibration complete — prediction accuracy evaluated." >> $GITHUB_STEP_SUMMARY
      - name: Commit forecast calibration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add logs/forecast_accuracy.json reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No forecast calibration changes to commit."
          else
            git commit -m "ci: calibrate predictive adaptation accuracy and update forecast reliability"
            git push
          fi
      - name: Append forecast summary
        run: |
          echo "Forecast reliability: ${{ steps.forecast.outputs.reliability }} (Accuracy: ${{ steps.forecast.outputs.accuracy }}%, Error: ${{ steps.forecast.outputs.error }}%)." >> $GITHUB_STEP_SUMMARY

  forecast-reliability-controller:
    needs: [calibrate-forecast-accuracy]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Apply forecast reliability feedback
        id: reliability
        run: |
          python scripts/workflow_utils/forecast_reliability_controller.py > reliability_out.json
          AVG_REL=$(jq -r '.avg_reliability' reliability_out.json || echo 1)
          REL_WEIGHT=$(jq -r '.reliability_weight' reliability_out.json || echo 1)
          LRF=$(jq -r '.learning_rate_factor' reliability_out.json || echo 1)
          CONF=$(jq -r '.confidence_threshold' reliability_out.json || echo 0)
          echo "avg_reliability=${AVG_REL}" >> $GITHUB_OUTPUT
          echo "reliability_weight=${REL_WEIGHT}" >> $GITHUB_OUTPUT
          echo "learning_rate_factor=${LRF}" >> $GITHUB_OUTPUT
          echo "confidence_threshold=${CONF}" >> $GITHUB_OUTPUT
          echo "🧭 Forecast reliability feedback applied — adaptation aggressiveness recalibrated." >> $GITHUB_STEP_SUMMARY
      - name: Commit reliability feedback changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add configs/governance_policy.json reports/audit_summary.md
          if git diff --cached --quiet; then
            echo "No reliability feedback changes to commit."
          else
            git commit -m "ci: adjust learning and confidence thresholds using forecast reliability feedback"
            git push
          fi
      - name: Append reliability summary
        run: |
          echo "Reliability weight: ${{ steps.reliability.outputs.reliability_weight }} (Avg: ${{ steps.reliability.outputs.avg_reliability }}). LRF: ${{ steps.reliability.outputs.learning_rate_factor }}; ConfThresh: ${{ steps.reliability.outputs.confidence_threshold }}." >> $GITHUB_STEP_SUMMARY

  governance-anomaly-detector:
    needs: [forecast-reliability-controller]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          cache-dependency-path: requirements.lock
      - name: Run governance anomaly detector
        id: anomaly
        run: |
          python scripts/workflow_utils/governance_anomaly_detector.py
          if [ ! -f governance_anomaly_out.json ]; then
            echo "::error::governance_anomaly_out.json not generated"
            exit 1
          fi
          ANOMALY_TOTAL=$(jq -r '.total' governance_anomaly_out.json || echo 0)
          echo "total=${ANOMALY_TOTAL}" >> $GITHUB_OUTPUT
          echo "🛡️ Governance anomaly detection complete. Total anomalies: ${ANOMALY_TOTAL}." >> $GITHUB_STEP_SUMMARY
      - name: Commit anomaly detection results
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add governance_anomaly_out.json
          if git diff --cached --quiet; then
            echo "No anomaly detection changes to commit."
          else
            git commit -m "ci: update governance anomaly detection results"
            git push
          fi
      - name: Append anomaly summary
        run: |
          echo "Governance anomalies detected: ${{ steps.anomaly.outputs.total }}." >> $GITHUB_STEP_SUMMARY
